{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn job_tracker.wsgi:application --bind 0.0.0.0:8000 --workers 1 --timeout 120",
    "restartPolicyType": "ON_FAILURE"
  }
}
